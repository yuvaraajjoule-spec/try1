# dYdX v4 Python client — pinned to 1.1.0 (known good, avoids compromised 1.1.5.post1)
dydx-v4-client==1.1.0
# NOTE: protobuf and cosmpy are intentionally NOT pinned here.
# cosmpy (pulled by dydx-v4-client) requires protobuf<6.0
# Letting pip resolve the correct compatible versions automatically.

# Data processing (SMC logic)
pandas>=2.0.0
numpy>=1.24.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP & async
aiohttp>=3.9.0
requests>=2.31.0

# Logging
colorlog>=6.7.0

# Telegram bot
python-telegram-bot>=20.7

# Scheduling (optional utility)
schedule>=1.2.0

# Keep-alive web server (for Render + UptimeRobot)
flask>=3.0.0
