# dYdX v4 Python client (avoid 1.1.5post1 — compromised supply chain version)
dydx-v4-client>=0.4.0,!=1.1.5.post1

# Protobuf — must match gencode version used by v4_proto (6.31.1)
# Error from Render: "gencode 6.31.1 runtime 5.29.6" → bump to >=6.31.1
protobuf>=6.31.1

# Cosmos / crypto primitives (bundled with dydx-v4-client, listed for clarity)
cosmpy>=0.9.0

# Data processing (SMC logic)
pandas>=2.0.0
numpy>=1.24.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP & async
aiohttp>=3.9.0
requests>=2.31.0

# Logging
colorlog>=6.7.0

# Telegram bot
python-telegram-bot>=20.7

# Scheduling (optional utility)
schedule>=1.2.0

# Keep-alive web server (for Render + UptimeRobot)
flask>=3.0.0
